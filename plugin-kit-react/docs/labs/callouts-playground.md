# Callouts Playground

This is a temporary page for testing native VitePress / GitHub-style markdown alerts without adding anything to the main sidebar.

Use it as a scratchpad while we lock in the theme styling for GitHub alert markup.

## Note

> [!NOTE]
> Use notes for neutral supporting context, like setup assumptions, cross-links, or implementation details that help without implying urgency.

## Tip

> [!TIP]
> Looking for custom schema fields? Start with the registry-driven field docs under [Schema Fields](/forms/schema-fields/text-field) and adapt the same patterns for your own components.

## Warning

> [!WARNING]
> Shadow DOM mounts need their own styles injected or inlined. If you forget that step, components can render correctly but appear unstyled.

## Important

> [!IMPORTANT]
> Prefer the canonical API surface for new integrations instead of keeping a legacy and a modern path alive in parallel.
>
> Prefer the canonical API surface for new integrations instead of keeping a legacy and a modern path alive in parallel.

## Caution

> [!CAUTION]
> Do not rely on deprecated host APIs in new integrations. Remove the older path entirely instead of shipping two parallel implementations.
