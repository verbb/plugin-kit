import { CSSResultGroup } from 'lit';
/**
 * Share constructable stylesheets across component instances ( pattern).
 * Falls back to an injected `<style>` tag when adoptedStyleSheets aren't supported.
 */
export declare function adoptStyles(shadowRoot: ShadowRoot, styleResults: CSSResultGroup[], cacheKey?: string): void;
export declare const hostDisplayInlineBlock: import('lit').CSSResult;
export declare const focusRing: import('lit').CSSResult;
//# sourceMappingURL=styles.d.ts.map