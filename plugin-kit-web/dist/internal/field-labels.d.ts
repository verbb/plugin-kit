/** Reads the legacy `hint` attribute when `instructions` is empty. */
export declare function readLegacyInstructions(element: HTMLElement, instructions: string): string;
/** Whether a named slot (or legacy alias) has content. */
export declare function hasInstructionContent(hasSlot: (name: string, flag?: boolean) => boolean, instructions: string, withInstructions?: boolean): boolean;
//# sourceMappingURL=field-labels.d.ts.map