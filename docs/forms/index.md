# Forms

Schema-driven forms for Craft CP screens (`SchemaForm`, `$field`, `$cmp`).

Start with the [SchemaForm overview](./overview), then the [API reference](./api/form-apis) or [Build a Settings Screen](./recipes/build-a-settings-screen) recipe.
