# Overview

The React package overview lives at the section root: **[React overview](/react/)**.

That page covers what `@verbb/plugin-kit-react` gives you and where to start (`createRoot` + `PluginKitProvider`). Continue with [Quick Start](./quick-start) when you are ready to wire a Craft CP screen.
