import {
    chevronDown,
    download,
    ellipsis,
    eye,
    flagCheckered,
    grip,
    house,
    list,
    pen,
    plus,
    search,
    share,
    asterisk,
    copy,
    clipboard,
    xmark,
    arrowUp,
    arrowDown,
    arrowLeft,
    arrowRight,
    bold,
    italic,
    underline,
    alignLeft,
    alignCenter,
    alignRight,
} from '@verbb/plugin-kit-icons';
import { renderIconHtml } from '@verbb/plugin-kit-web/icons';

export const playgroundIconEye = renderIconHtml(eye);

export const playgroundIconAdd = renderIconHtml(plus);

export const playgroundIconPen = renderIconHtml(pen);

export const playgroundIconSearch = renderIconHtml(search);

export const playgroundIconHouse = renderIconHtml(house);

export const playgroundIconFlag = renderIconHtml(flagCheckered);

export const playgroundIconDownload = renderIconHtml(download);

export const playgroundIconChevronDown = renderIconHtml(chevronDown);

export const playgroundIconEllipsis = renderIconHtml(ellipsis);

export const playgroundIconList = renderIconHtml(list);

/** Craft CP cards view stand-in — Plugin Kit glyph is `grip`. */
export const playgroundIconNameElementCards = 'grip' as const;

export const playgroundIconElementCards = renderIconHtml(grip);

export const playgroundIconShare = renderIconHtml(share);

export const playgroundIconAsterisk = renderIconHtml(asterisk);

export const playgroundIconCopy = renderIconHtml(copy);

export const playgroundIconClipboard = renderIconHtml(clipboard);

export const playgroundIconXmark = renderIconHtml(xmark);

export const playgroundIconArrowUp = renderIconHtml(arrowUp);

export const playgroundIconArrowDown = renderIconHtml(arrowDown);

export const playgroundIconArrowLeft = renderIconHtml(arrowLeft);

export const playgroundIconArrowRight = renderIconHtml(arrowRight);

export const playgroundIconBold = renderIconHtml(bold);

export const playgroundIconItalic = renderIconHtml(italic);

export const playgroundIconUnderline = renderIconHtml(underline);

export const playgroundIconAlignLeft = renderIconHtml(alignLeft);

export const playgroundIconAlignCenter = renderIconHtml(alignCenter);

export const playgroundIconAlignRight = renderIconHtml(alignRight);
